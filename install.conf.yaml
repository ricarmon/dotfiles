- defaults:
    link:
      create: true
      relink: true

- clean: ['~']


- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig


- shell:
  - [git submodule update --init --recursive, Installing submodules]

- sudo:
  - aptget: flameshot
